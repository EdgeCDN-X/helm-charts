apiVersion: v1
kind: Service
metadata:
  name: {{ include "edgeottx-media-processor.fullname" . }}
  labels:
    {{- include "edgeottx-media-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edgeottx-media-processor.selectorLabels" . | nindent 4 }}

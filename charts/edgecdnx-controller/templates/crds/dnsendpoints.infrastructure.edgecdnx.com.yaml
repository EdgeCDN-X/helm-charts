---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: dnsendpoints.infrastructure.edgecdnx.com
spec:
  group: infrastructure.edgecdnx.com
  names:
    kind: DNSEndpoint
    listKind: DNSEndpointList
    plural: dnsendpoints
    singular: dnsendpoint
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DNSEndpoint is the Schema for the dnsendpoints API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DNSEndpointSpec defines the desired state of DNSEndpoint.
            properties:
              dnsName:
                description: DNSName is the fully qualified domain name for the DNSEndpoint.
                  Edit dnsendpoint_types.go to remove/update
                type: string
              recordTTL:
                description: RecordTTL specifies the time-to-live for the DNS record
                  in seconds. Edit dnsendpoint_types.go to remove/update
                type: integer
              recordType:
                description: RecordType specifies the type of DNS record for the DNSEndpoint.
                  Edit dnsendpoint_types.go to remove/update
                enum:
                - A
                - AAAA
                - CNAME
                - TXT
                - MX
                - SRV
                - NS
                type: string
              routeSelector:
                description: |-
                  RouteSelector specifies the label selector for routing traffic to the appropriate endpoints. Edit dnsendpoint_types.go to remove/update
                  Used with Geolocation, Weighted, RoundRobin routing policies.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              routingPolicy:
                description: RoutingPolicy specifies the routing policy for the DNSEndpoint.
                  Edit dnsendpoint_types.go to remove/update
                enum:
                - Simple
                - Weighted
                - Failover
                - Geolocation
                - RoundRobin
                type: string
              targets:
                description: |-
                  Targets specifies the list of target endpoints for the DNSEndpoint. Edit dnsendpoint_types.go to remove/update
                  Used with Simple and Failover routing policies.
                items:
                  type: string
                type: array
            type: object
          status:
            description: DNSEndpointStatus defines the observed state of DNSEndpoint.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "request-analyser.fullname" . }}
  labels:
    {{- include "request-analyser.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "request-analyser.selectorLabels" . | nindent 4 }}

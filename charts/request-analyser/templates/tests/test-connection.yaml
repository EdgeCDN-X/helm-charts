apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "request-analyser.fullname" . }}-test-connection"
  labels:
    {{- include "request-analyser.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "request-analyser.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

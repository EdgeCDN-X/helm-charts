apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edgecdnx-website.fullname" . }}-test-connection"
  labels:
    {{- include "edgecdnx-website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edgecdnx-website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

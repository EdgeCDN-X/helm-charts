apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "edgecdnx-website.fullname" . }}-config
  labels:
    {{- include "edgecdnx-website.labels" . | nindent 4 }}
data:
  runtime-config.json: |
    {{- toJson .Values.config | nindent 4 }}

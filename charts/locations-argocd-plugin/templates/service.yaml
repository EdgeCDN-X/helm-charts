apiVersion: v1
kind: Service
metadata:
  name: {{ include "locations-argocd-plugin.fullname" . }}
  labels:
    {{- include "locations-argocd-plugin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "locations-argocd-plugin.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "edgeottx.fullname" . }}-config
  labels:
    {{- include "edgeottx.labels" . | nindent 4 }}
data:
  app-config.json: |
    {{- toJson .Values.config | nindent 4 }}

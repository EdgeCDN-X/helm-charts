apiVersion: v1
kind: Service
metadata:
  name: {{ include "edgeottx.fullname" . }}
  labels:
    {{- include "edgeottx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edgeottx.selectorLabels" . | nindent 4 }}

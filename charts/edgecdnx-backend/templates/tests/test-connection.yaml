apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edgecdnx-backend.fullname" . }}-test-connection"
  labels:
    {{- include "edgecdnx-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edgecdnx-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

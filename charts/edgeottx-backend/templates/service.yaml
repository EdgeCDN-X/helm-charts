apiVersion: v1
kind: Service
metadata:
  name: {{ include "edgeottx-backend.fullname" . }}
  labels:
    {{- include "edgeottx-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edgeottx-backend.selectorLabels" . | nindent 4 }}
